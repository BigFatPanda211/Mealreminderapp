#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../@grpc/proto-loader/build/bin/proto-loader-gen-types.js" "$@"
else
  exec node  "$basedir/../../../../@grpc/proto-loader/build/bin/proto-loader-gen-types.js" "$@"
fi
# cmd-shim-target=C:/Users/Asad/StudioProjects/Mealreminderapp/node_modules/.pnpm/@firebase+firestore@4.14.1_@firebase+app@0.14.12/node_modules/@grpc/proto-loader/build/bin/proto-loader-gen-types.js
